console.log(name);

var name = "Shivaraj";


if (true) {
    let lastName = "Loni";   // "let" is allowed & executes only in that scope! or in that block only 
    console.log(lastName);
}
// console.log(lastName);    // using "var" keyword!, this can be accessed & prints the result, 

for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
}